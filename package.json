{
  "name": "reactive-hydration-root",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/reactive-hydration-root && yarn build && cd ../example-nextjs && yarn build",
    "postinstall": "cd packages/reactive-hydration && yarn link && cd ../example-nextjs && yarn link reactive-hydration"
  },
  "devDependencies": {
    "eslint": "8.22.0",
    "typescript": "4.7.4"
  }
}
