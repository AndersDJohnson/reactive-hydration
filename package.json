{
  "name": "reactive-hydration-root",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npx turbo run build",
    "lint": "npx turbo run --exclude reactive-hydration-root eslint src --ext ts --ext tsx",
    "postinstall": "rm -rf packages/*/node_modules/{react,react-dom,recoil,recoil-nexus}"
  },
  "devDependencies": {
    "turbo": "^1.4.6"
  }
}
